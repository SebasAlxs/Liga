export interface CreateTeamDTO {
    name: string;
    city: string;
    foundedYear: number;
}

export interface TeamResponseDTO {
    id: string;
    name: string;
    city: string;
    foundedYear: number;
}
